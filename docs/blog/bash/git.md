# Git commands

## Git operations with key

Get an access token 

```shell
username=""
repository="
ACCESS_TOKEN=""
git push "https://${ACCESS_TOKEN}@github.com/${username}/${repository}.git"
```

## Merge master to current branch

```shell
git merge origin master
```
